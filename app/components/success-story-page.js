import Component from '@ember/component';

export default Component.extend({
  // attrs.
  stories: null,

  // init(args) {
  //   this._super(args);
  //   console.log(this.stories.firstObject.bodyJson);
  //   JSON.parse(this.stories.firstObject.bodyJson);
  // }
});
